# Point the renderer's Shell daemon check at our blueprint server (:7777)
# instead of the dead Phoenix daemon (:4488). The blueprint server exposes
# /api/health as an alias so DAEMON_HEALTH_URL resolves correctly.
VITE_EMA_DAEMON_URL=http://127.0.0.1:7777
